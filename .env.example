# This is an example file. Copy it to a new file named .env
# and fill in your actual credentials. The .env file should NOT be committed to git.

# Replace with your actual MongoDB connection string from Atlas
CONNECTION_URI="mongodb+srv://<user>:<password>@<cluster-url>/<databaseName>?retryWrites=true&w=majority"

# Create a strong, unique secret for signing JSON Web Tokens (JWT)
JWT_SECRET="your_super_secret_key_that_is_long_and_random"
